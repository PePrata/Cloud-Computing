project_name = "shop"
# No hosted_zone_id / dns_name here: this build has no owned/delegated
# public domain, so Route 53 client-facing DNS failover records are
# out of scope (see docs/limitations.md). Health-check-driven alarm +
# Lambda promotion of the standby replica are unaffected.
